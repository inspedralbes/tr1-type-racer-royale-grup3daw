# ===================================================================
#   PLANTILLA PARA VARIABLES DE ENTORNO DEL FRONTEND (VUE/VITE)
# ===================================================================

# Entorno de la aplicación (development | production)
# Vite usa `import.meta.env.MODE` internamente, pero si quieres una variable explícita,
# debe empezar con VITE_ para ser accesible en el código.
VITE_NODE_ENV=development

# URL base del servidor backend.
# El código de Vue (usando Axios, Fetch o Socket.IO) debe usar esta variable
# para saber a dónde enviar las peticiones de API y las conexiones de socket.
VITE_API_URL=

# URL del servidor de sockets (si es diferente de la API).
VITE_SOCKET_URL=
